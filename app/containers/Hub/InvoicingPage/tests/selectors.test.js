// import { selectInvoicingPageDomain } from '../selectors';

describe('selectInvoicingPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
