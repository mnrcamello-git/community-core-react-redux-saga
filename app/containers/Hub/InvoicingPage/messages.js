/*
 * InvoicingPage Messages
 *
 * This contains all the text for the InvoicingPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InvoicingPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InvoicingPage container!',
  },
});
