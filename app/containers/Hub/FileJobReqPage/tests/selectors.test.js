// import { selectFileJobReqPageDomain } from '../selectors';

describe('selectFileJobReqPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
