/*
 * FaqPage Messages
 *
 * This contains all the text for the FaqPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FaqPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Hi! We are here to help you',
  },
});
