// import { selectAgreementPageDomain } from '../selectors';

describe('selectAgreementPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
