/*
 * AgreementPage Messages
 *
 * This contains all the text for the AgreementPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AgreementPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AgreementPage container!',
  },
});
