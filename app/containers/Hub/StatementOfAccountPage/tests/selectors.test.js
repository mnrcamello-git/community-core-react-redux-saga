// import { selectStatementOfAccountPageDomain } from '../selectors';

describe('selectStatementOfAccountPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
