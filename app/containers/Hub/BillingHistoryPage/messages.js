/*
 * BillingHistoryPage Messages
 *
 * This contains all the text for the BillingHistoryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BillingHistoryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BillingHistoryPage container!',
  },
});
