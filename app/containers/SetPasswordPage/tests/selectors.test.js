// import { selectSetPasswordPageDomain } from '../selectors';

describe('selectSetPasswordPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
