// import { selectCustomerServicePageDomain } from '../selectors';

describe('selectCustomerServicePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
