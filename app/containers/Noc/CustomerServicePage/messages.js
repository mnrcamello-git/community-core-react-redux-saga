/*
 * CustomerServicePage Messages
 *
 * This contains all the text for the CustomerServicePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomerServicePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomerServicePage container!',
  },
});
