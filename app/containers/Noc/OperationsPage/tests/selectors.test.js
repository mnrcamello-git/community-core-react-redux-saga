// import { selectOperationsPageDomain } from '../selectors';

describe('selectOperationsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
