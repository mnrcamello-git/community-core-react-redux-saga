/*
 * InterviewNotesModal Messages
 *
 * This contains all the text for the InterviewNotesModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.InterviewNotesModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InterviewNotesModal component!',
  },
});
