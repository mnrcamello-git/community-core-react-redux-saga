/*
 * JobOpeningModal Messages
 *
 * This contains all the text for the JobOpeningModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.JobOpeningModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the JobOpeningModal component!',
  },
});
