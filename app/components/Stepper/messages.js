/*
 * Stepper Messages
 *
 * This contains all the text for the Stepper component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Stepper';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Stepper component!',
  },
});
