/*
 * UserCreationModal Messages
 *
 * This contains all the text for the UserCreationModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserCreationModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserCreationModal component!',
  },
});
