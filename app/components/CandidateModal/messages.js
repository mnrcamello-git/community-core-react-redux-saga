/*
 * CandidateModal Messages
 *
 * This contains all the text for the CandidateModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CandidateModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CandidateModal component!',
  },
});
